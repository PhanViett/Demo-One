import { enableProdMode } from '@angular/core';
import { SrvRecord } from 'dns';

export class Employee{
    id : number;
    name : string;
    gender : string;
    position : string;
    email : string;
    active : boolean
    
}